# Authenticity Contract #7 — the format tag `(G)` shown in parentheses
# on control-panel line 1 for numeric cells (numbers and formulas).
# Labels and empty cells show no tag.
#
# M2 hard-codes General format for numeric cells; per-cell formats
# arrive with /Range Format in M3.

ASSERT_POINTER    A:A1
ASSERT_PANEL_L1   A:A1:

# Number → (G) appears before the value
KEYS 42
ENTER
ASSERT_PANEL_L1   A:A1: (G) 42

# Formula → (G) appears before the source
DOWN
KEY @
KEYS SUM(A1..A1)
ENTER
ASSERT_PANEL_L1   A:A2: (G) @SUM(A1..A1)

# Label → no tag
DOWN
KEYS hi
ENTER
ASSERT_PANEL_L1   A:A3: 'hi
