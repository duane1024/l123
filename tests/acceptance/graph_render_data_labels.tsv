# /Graph Options Data-Labels — render the per-series label text in
# the F10 line view at the chosen placement.
#
# Slice F (data labels render) of GRAPH_PLAN.md. The previous slice
# wired the placement submenu and stored Center/Left/Above/Right/
# Below per slot but the renderer ignored the stored data-label
# range. After: cell text from `data_labels[0]` is read, threaded
# through `GraphValues.data_label_text[0]`, and painted near each
# A-series `•` dot at the placement offset.
# Reference p. 2-217.

SIZE 80 30

# Seed A1..A4 = 1, 2, 3, 4 (line series).
KEYS 1
DOWN
KEYS 2
DOWN
KEYS 3
DOWN
KEYS 4
DOWN
HOME

# Seed B1..B4 = Q1..Q4 (label text).
RIGHT
KEYS Q1
DOWN
KEYS Q2
DOWN
KEYS Q3
DOWN
KEYS Q4
DOWN
HOME

# /GTL — line graph type.
KEY /
KEY G
KEY T
KEY L

# /GA over A1..A4.
KEY /
KEY G
KEY A
DOWN
DOWN
DOWN
ENTER
HOME

# /GOD A over B1..B4 with placement Above.
RIGHT
KEY /
KEY G
KEY O
KEY D
KEY A
DOWN
DOWN
DOWN
ENTER
KEY A
ASSERT_MODE          READY
ASSERT_GRAPH_DATA_LABELS  A          A:B1..A:B4
ASSERT_GRAPH_DATA_LABELS_PLACEMENT  A  Above

# F10 — labels render alongside the line plot. The fourth dot
# (Q4 = 4 = max) sits at the top edge, so its Above-placed label
# would land off-screen and is suppressed by `paint_data_label`.
# Q1..Q3 render as expected.
F 10
ASSERT_MODE          GRAPH
ASSERT_SCREEN        •
ASSERT_SCREEN        Q1
ASSERT_SCREEN        Q2
ASSERT_SCREEN        Q3

ESC
ASSERT_MODE          READY
