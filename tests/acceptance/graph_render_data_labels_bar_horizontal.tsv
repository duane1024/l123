# /Graph Options Data-Labels — render label text in the F10
# horizontal-bar view at the chosen placement.
#
# Slice F (data labels render — bar horizontal) of GRAPH_PLAN.md.
# Each horizontal bar gets the matching A-slot data-label cell
# text painted at the placement offset relative to the bar's
# right end.
# Reference p. 2-217.

SIZE 80 30

# Seed A1..A4 = 1, 2, 3, 4 (bar series).
KEYS 1
DOWN
KEYS 2
DOWN
KEYS 3
DOWN
KEYS 4
DOWN
HOME

# Seed B1..B4 = Q1..Q4 (label text).
RIGHT
KEYS Q1
DOWN
KEYS Q2
DOWN
KEYS Q3
DOWN
KEYS Q4
DOWN
HOME

# /GTB — bar graph type, then /GTFH for Horizontal orientation.
KEY /
KEY G
KEY T
KEY B
KEY /
KEY G
KEY T
KEY F
KEY H

# /GA over A1..A4.
KEY /
KEY G
KEY A
DOWN
DOWN
DOWN
ENTER
HOME

# /GOD A over B1..B4 with placement Right.
RIGHT
KEY /
KEY G
KEY O
KEY D
KEY A
DOWN
DOWN
DOWN
ENTER
KEY R
ASSERT_MODE          READY
ASSERT_GRAPH_DATA_LABELS  A          A:B1..A:B4
ASSERT_GRAPH_DATA_LABELS_PLACEMENT  A  Right

# F10 — labels render at the right end of each horizontal bar.
# The fourth bar (Q4 = 4) is the max value, so its bar fills the
# plot's full width, leaving no room for the Right-placed label.
# Q4's label is clipped — the renderer doesn't reserve space for
# data labels. Q1..Q3 render as expected since their bars are
# shorter than the plot width.
F 10
ASSERT_MODE          GRAPH
ASSERT_SCREEN        █
ASSERT_SCREEN        Q1
ASSERT_SCREEN        Q2
ASSERT_SCREEN        Q3

ESC
ASSERT_MODE          READY
